/// <reference types="react" />
/** @jsx jsx */
import Highlight from 'prism-react-renderer';
import { jsx } from 'theme-ui';
declare type HighlightProps = React.ComponentPropsWithoutRef<typeof Highlight>;
export interface ThemeUIPrismProps extends Omit<HighlightProps, 'children' | 'code' | 'language' | 'theme' | 'Prism'> {
    className: string;
    children: string;
    Prism?: HighlightProps['Prism'];
}
export default function ThemeUIPrism({ children, className: outerClassName, ...props }: ThemeUIPrismProps): jsx.JSX.Element;
export {};
